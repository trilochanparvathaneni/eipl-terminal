# ── Database ────────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://eipl:eipl_secret@localhost:5432/eipl_terminal?schema=public"

# ── Auth (NextAuth) ──────────────────────────────────────────────────────────
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="super-secret-change-in-production"
# Must match your deployment URL exactly (no trailing slash)
NEXTAUTH_URL="http://localhost:3000"

# ── OpenAI (AI Chat & Document Form Extraction) ──────────────────────────────
# Required at RUNTIME (not build time). Set this in your Vercel project's
# Environment Variables under Settings → Environment Variables.
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4.1-mini"
CHAT_RATE_LIMIT_RPM="30"
